<div id="tab-attendance" class="tab-content hidden p-8">

    <div class="mb-6">
        <h2 class="text-xl font-bold text-gray-800 dark:text-white">Attendance Capture Settings</h2>
        <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Configure how employees clock in and out across your organization.</p>
    </div>

    <hr class="mb-8 border-gray-200 dark:border-gray-700" />

    <form id="org-attendance-settings-form">

        <div id="attendanceSettingsError" class="mb-4 hidden rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700 dark:border-red-500/40 dark:bg-red-900/30 dark:text-red-200"></div>

        @* --- SECTION 1: PRIMARY MODE ------------------------------- *@
        <div class="mb-8">
            <label class="mb-1 block text-xs font-bold tracking-wide text-gray-500 uppercase dark:text-gray-400">Primary Mode</label>
            <p class="mb-4 text-xs text-gray-400 dark:text-gray-500">Select the main method employees use to record attendance.</p>

            <div class="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4">

                @* QR / Kiosk *@
                <label class="group relative cursor-pointer">
                    <input type="radio" name="primaryMode" class="peer sr-only" value="QR" checked>
                    <div class="flex flex-col items-start gap-2 rounded-xl border border-gray-200 bg-white p-4 shadow-sm transition-all
                                peer-checked:border-emerald-400 peer-checked:bg-emerald-50/60 peer-checked:ring-1 peer-checked:ring-emerald-300
                                hover:border-emerald-300 hover:shadow-md
                                dark:border-gray-700 dark:bg-gray-800 dark:peer-checked:border-emerald-500 dark:peer-checked:bg-emerald-900/20">
                        <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-gray-100 text-gray-500 transition-colors peer-checked:bg-emerald-100 peer-checked:text-emerald-600 dark:bg-gray-700 dark:peer-checked:bg-emerald-900/40 dark:peer-checked:text-emerald-400 group-has-[input:checked]:bg-emerald-100 group-has-[input:checked]:text-emerald-600 dark:group-has-[input:checked]:bg-emerald-900/40 dark:group-has-[input:checked]:text-emerald-400">
                            <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 013.75 9.375v-4.5zM3.75 14.625c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5a1.125 1.125 0 01-1.125-1.125v-4.5zM13.5 4.875c0-.621.504-1.125 1.125-1.125h4.5c.621 0 1.125.504 1.125 1.125v4.5c0 .621-.504 1.125-1.125 1.125h-4.5A1.125 1.125 0 0113.5 9.375v-4.5z" />
                                <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 6.75h.75v.75h-.75v-.75zM6.75 16.5h.75v.75h-.75v-.75zM16.5 6.75h.75v.75h-.75v-.75zM13.5 13.5h.75v.75h-.75v-.75zM13.5 19.5h.75v.75h-.75v-.75zM19.5 13.5h.75v.75h-.75v-.75zM19.5 19.5h.75v.75h-.75v-.75zM16.5 16.5h.75v.75h-.75v-.75z" />
                            </svg>
                        </div>
                        <div>
                            <span class="block text-sm font-semibold text-gray-800 dark:text-white">QR / Kiosk</span>
                            <span class="text-xs text-gray-500 dark:text-gray-400">Scan-based clock-in</span>
                        </div>
                    </div>
                </label>

                @* Biometric + Geo *@
                <label class="group relative cursor-pointer">
                    <input type="radio" name="primaryMode" class="peer sr-only" value="Biometric">
                    <div class="flex flex-col items-start gap-2 rounded-xl border border-gray-200 bg-white p-4 shadow-sm transition-all
                                peer-checked:border-emerald-400 peer-checked:bg-emerald-50/60 peer-checked:ring-1 peer-checked:ring-emerald-300
                                hover:border-emerald-300 hover:shadow-md
                                dark:border-gray-700 dark:bg-gray-800 dark:peer-checked:border-emerald-500 dark:peer-checked:bg-emerald-900/20">
                        <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-gray-100 text-gray-500 transition-colors group-has-[input:checked]:bg-emerald-100 group-has-[input:checked]:text-emerald-600 dark:bg-gray-700 dark:group-has-[input:checked]:bg-emerald-900/40 dark:group-has-[input:checked]:text-emerald-400">
                            <svg class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M7.864 4.243A7.5 7.5 0 0 1 19.5 10.5c0 2.92-.556 5.709-1.568 8.268M5.742 6.364A7.465 7.465 0 0 0 4.5 10.5a7.464 7.464 0 0 1-1.15 3.993m1.989 3.559A11.209 11.209 0 0 0 8.25 10.5a3.75 3.75 0 1 1 7.5 0c0 .527-.021 1.049-.064 1.565M12 10.5a14.94 14.94 0 0 1-3.6 9.75m6.633-4.596a18.666 18.666 0 0 1-2.485 5.33" />
                            </svg>
                        </div>
                        <div>
                            <span class="block text-sm font-semibold text-gray-800 dark:text-white">Biometric + Geo</span>
                            <span class="text-xs text-gray-500 dark:text-gray-400">Face/finger + location</span>
                        </div>
                    </div>
                </label>

                @* Hybrid *@
                <label class="group relative cursor-pointer">
                    <input type="radio" name="primaryMode" class="peer sr-only" value="Hybrid">
                    <div class="flex flex-col items-start gap-2 rounded-xl border border-gray-200 bg-white p-4 shadow-sm transition-all
                                peer-checked:border-emerald-400 peer-checked:bg-emerald-50/60 peer-checked:ring-1 peer-checked:ring-emerald-300
                                hover:border-emerald-300 hover:shadow-md
                                dark:border-gray-700 dark:bg-gray-800 dark:peer-checked:border-emerald-500 dark:peer-checked:bg-emerald-900/20">
                        <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-gray-100 text-gray-500 transition-colors group-has-[input:checked]:bg-emerald-100 group-has-[input:checked]:text-emerald-600 dark:bg-gray-700 dark:group-has-[input:checked]:bg-emerald-900/40 dark:group-has-[input:checked]:text-emerald-400">
                            <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z" />
                                <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z" />
                            </svg>
                        </div>
                        <div>
                            <span class="block text-sm font-semibold text-gray-800 dark:text-white">Hybrid</span>
                            <span class="text-xs text-gray-500 dark:text-gray-400">QR + Bio, employee's choice</span>
                        </div>
                    </div>
                </label>

                @* Biometric + Geo + Disabled Mode placeholder (future) � omit for now *@

            </div>
        </div>

        @* --- SECTION 2: MANUAL ENTRY ------------------------------ *@
        <div class="mb-8 overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm dark:border-gray-700 dark:bg-gray-800">

            <div class="flex items-center justify-between border-b border-gray-100 px-5 py-4 dark:border-gray-700">
                <div>
                    <span class="block text-sm font-semibold text-gray-800 dark:text-white">Manual Entry</span>
                    <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">Allow time logs to be submitted without a QR scan or biometric capture.</p>
                </div>
                <label class="relative inline-flex cursor-pointer items-center">
                    <input type="checkbox" id="manualEntryToggle" class="peer sr-only" onchange="toggleManualOptions(this)">
                    <div class="h-6 w-11 rounded-full bg-gray-200 after:absolute after:top-[2px] after:left-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-['']
                                peer-checked:bg-emerald-500 peer-checked:after:translate-x-full peer-checked:after:border-white
                                peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-emerald-500/30
                                dark:bg-gray-600 dark:border-gray-500"></div>
                </label>
            </div>

            @* Manual Entry Sub-options (shown when enabled) *@
            <div id="manualEntryOptions" class="hidden px-5 py-4 bg-gray-50 dark:bg-gray-700/30">
                <p class="mb-3 text-xs font-semibold text-gray-500 uppercase tracking-wide dark:text-gray-400">Who can submit manual entries?</p>
                <div class="flex flex-col gap-2 sm:flex-row sm:gap-4">

                    <label class="flex cursor-pointer items-center gap-2.5">
                        <input type="radio" name="manual_entry_access" value="admin" class="h-4 w-4 accent-emerald-500">
                        <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Admin Only</span>
                    </label>

                    <label class="flex cursor-pointer items-center gap-2.5">
                        <input type="radio" name="manual_entry_access" value="supervisor" checked class="h-4 w-4 accent-emerald-500">
                        <span class="text-sm font-medium text-gray-700 dark:text-gray-300">Supervisor Approval Required</span>
                    </label>

                </div>
            </div>

        </div>

        @* --- SECTION 3: RESTRICTIONS ------------------------------- *@
        <div class="mb-2">
            <label class="mb-1 block text-xs font-bold tracking-wide text-gray-500 uppercase dark:text-gray-400">Restrictions</label>
            <p class="mb-4 text-xs text-gray-400 dark:text-gray-500">Fine-tune guard rails for attendance logging.</p>
        </div>

        <div class="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm dark:border-gray-700 dark:bg-gray-800">

            @* Require Geofence *@
            <div class="flex items-center justify-between border-b border-gray-100 px-5 py-4 dark:border-gray-700">
                <div>
                    <span class="block text-sm font-semibold text-gray-800 dark:text-white">Require Geofence</span>
                    <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">Employee must be within the designated location radius to clock in.</p>
                </div>
                <label class="relative inline-flex cursor-pointer items-center">
                     <input type="checkbox" id="requireGeofenceToggle" class="peer sr-only">
                    <div class="h-6 w-11 rounded-full bg-gray-200 after:absolute after:top-[2px] after:left-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-['']
                                peer-checked:bg-emerald-500 peer-checked:after:translate-x-full peer-checked:after:border-white
                                peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-emerald-500/30
                                dark:bg-gray-600 dark:border-gray-500"></div>
                </label>
            </div>

            @* Restrict by IP *@
            <div class="flex items-center justify-between border-b border-gray-100 px-5 py-4 dark:border-gray-700">
                <div>
                    <span class="block text-sm font-semibold text-gray-800 dark:text-white">Restrict by IP</span>
                    <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">Only allow clock-ins from approved IP addresses (e.g. office Wi-Fi).</p>
                </div>
                <label class="relative inline-flex cursor-pointer items-center">
                     <input type="checkbox" id="restrictByIpToggle" class="peer sr-only">
                    <div class="h-6 w-11 rounded-full bg-gray-200 after:absolute after:top-[2px] after:left-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-['']
                                peer-checked:bg-emerald-500 peer-checked:after:translate-x-full peer-checked:after:border-white
                                peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-emerald-500/30
                                dark:bg-gray-600 dark:border-gray-500"></div>
                </label>
            </div>

            @* Prevent Double Clock-in *@
            <div class="flex items-center justify-between border-b border-gray-100 px-5 py-4 dark:border-gray-700">
                <div>
                    <span class="block text-sm font-semibold text-gray-800 dark:text-white">Prevent Double Clock-in</span>
                    <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">Block employees from clocking in again while already clocked in.</p>
                </div>
                <label class="relative inline-flex cursor-pointer items-center">
                     <input type="checkbox" id="preventDoubleClockInToggle" class="peer sr-only" checked>
                    <div class="h-6 w-11 rounded-full bg-gray-200 after:absolute after:top-[2px] after:left-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-['']
                                peer-checked:bg-emerald-500 peer-checked:after:translate-x-full peer-checked:after:border-white
                                peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-emerald-500/30
                                dark:bg-gray-600 dark:border-gray-500"></div>
                </label>
            </div>

            @* Auto Clock-out *@
            <div class="flex items-start justify-between px-5 py-4" id="autoClockOutRow">
                <div class="flex-1 pr-8">
                    <span class="block text-sm font-semibold text-gray-800 dark:text-white">Auto Clock-out After Max Hours</span>
                    <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">Automatically clock out employees who exceed the set hour limit.</p>

                    @* Inline hour input, visible when toggle is on *@
                    <div id="autoClockOutInput" class="mt-3 hidden">
                        <div class="flex items-center gap-2">
                            <input type="number" id="autoClockOutHours" min="1" max="24" step="0.25" value="12" disabled
                                    class="w-20 rounded-lg border-0 py-1.5 px-3 text-sm text-gray-900 ring-1 ring-inset ring-gray-300 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:bg-gray-900 dark:text-white dark:ring-gray-600"
                                    placeholder="12">
                            <span class="text-sm text-gray-500 dark:text-gray-400">hours max per shift</span>
                        </div>
                    </div>
                </div>
                <label class="relative mt-0.5 inline-flex cursor-pointer items-center">
                    <input type="checkbox" id="autoClockOutToggle" class="peer sr-only" onchange="toggleAutoClockOut(this)">
                    <div class="h-6 w-11 rounded-full bg-gray-200 after:absolute after:top-[2px] after:left-[2px] after:h-5 after:w-5 after:rounded-full after:border after:border-gray-300 after:bg-white after:transition-all after:content-['']
                                peer-checked:bg-emerald-500 peer-checked:after:translate-x-full peer-checked:after:border-white
                                peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-emerald-500/30
                                dark:bg-gray-600 dark:border-gray-500"></div>
                </label>
            </div>

        </div>

        @* --- DEFAULT SHIFT TEMPLATE -------------------------------- *@
        <div class="mt-8 overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm dark:border-gray-700 dark:bg-gray-800">
            <div class="px-5 py-4">
                <span class="block text-sm font-semibold text-gray-800 dark:text-white">Default Shift Template</span>
                <p class="mt-0.5 text-xs text-gray-500 dark:text-gray-400">Pick the fallback shift template for employees without explicit schedules.</p>

                <div class="mt-4 grid gap-3 sm:grid-cols-[minmax(0,320px)_1fr]">
                    <input type="text" id="defaultShiftTemplateCode" list="defaultShiftTemplateOptions" class="rounded-lg border border-gray-300 px-3 py-2 text-sm text-gray-900 focus:border-emerald-500 focus:outline-none focus:ring-2 focus:ring-emerald-500 dark:border-gray-700 dark:bg-gray-900 dark:text-white"
                           placeholder="e.g. DAYSHIFT_9_6">
                    <p class="text-xs text-gray-500 dark:text-gray-400">Leave blank to respect each employee's current assignment.</p>
                </div>
            </div>
        </div>

        <datalist id="defaultShiftTemplateOptions">
            <option value="DAYSHIFT_9_6">Standard Day (9 AM - 6 PM)</option>
            <option value="MIDSHIFT_2_11">Mid Shift (2 PM - 11 PM)</option>
            <option value="NIGHTSHIFT_10_7">Night Shift (10 PM - 7 AM)</option>
        </datalist>

        @* --- SAVE BUTTON -------------------------------------------- *@
        <hr class="mt-8 mb-6 border-gray-200 dark:border-gray-700" />

        <div class="flex justify-end">
            <button type="submit" id="attendanceSettingsSaveBtn" class="btn-primary">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-4 w-4">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                Save Attendance Settings
            </button>
        </div>

    </form>

</div>

<partial name="~/Pages/App/OrgConfig/Partials/Modals/_AttendanceInstructionModals.cshtml" />

<script>
    function openInstructionModal(modalId) {
        const modal = document.getElementById(modalId);
        if (!modal) return;
        modal.classList.remove('hidden');
        modal.setAttribute('aria-hidden', 'false');
        document.body.classList.add('overflow-hidden');
    }

    function closeInstructionModal(modalId) {
        const modal = document.getElementById(modalId);
        if (!modal) return;
        modal.classList.add('hidden');
        modal.setAttribute('aria-hidden', 'true');
        if (!document.querySelector('.instruction-modal:not(.hidden)')) {
            document.body.classList.remove('overflow-hidden');
        }
    }

    function toggleManualOptions(checkbox) {
        const opts = document.getElementById('manualEntryOptions');
        if (!opts || !checkbox) return;
        if (checkbox.checked) {
            opts.classList.remove('hidden');
        } else {
            opts.classList.add('hidden');
        }
    }

    function toggleAutoClockOut(checkbox) {
        const wrapper = document.getElementById('autoClockOutInput');
        const input = document.getElementById('autoClockOutHours');
        if (!wrapper || !checkbox) return;
        if (checkbox.checked) {
            wrapper.classList.remove('hidden');
            if (input) input.removeAttribute('disabled');
        } else {
            wrapper.classList.add('hidden');
            if (input) {
                input.setAttribute('disabled', 'true');
            }
        }
    }

    (function initAttendanceSettings() {
        const form = document.getElementById('org-attendance-settings-form');
        if (!form) return;

        const appRoot = document.getElementById('appRoot');
        const apiBase = (appRoot?.getAttribute('data-api-base') || '').trim().replace(/\/$/, '');
        const token = appRoot?.getAttribute('data-access-token') || '';
        if (!apiBase || !token) return;

        const manualToggle = document.getElementById('manualEntryToggle');
        const requireGeofenceToggle = document.getElementById('requireGeofenceToggle');
        const restrictByIpToggle = document.getElementById('restrictByIpToggle');
        const preventDoubleClockInToggle = document.getElementById('preventDoubleClockInToggle');
        const autoClockOutToggle = document.getElementById('autoClockOutToggle');
        const autoClockOutHours = document.getElementById('autoClockOutHours');
        const defaultShiftInput = document.getElementById('defaultShiftTemplateCode');
        const saveBtn = document.getElementById('attendanceSettingsSaveBtn');
        const errorBox = document.getElementById('attendanceSettingsError');

        let isSaving = false;

        function showError(message) {
            if (!errorBox) return;
            if (message) {
                errorBox.textContent = message;
                errorBox.classList.remove('hidden');
            } else {
                errorBox.textContent = '';
                errorBox.classList.add('hidden');
            }
        }

        function setSaving(state) {
            isSaving = state;
            if (saveBtn) {
                saveBtn.disabled = state;
                saveBtn.textContent = state ? 'Saving...' : 'Save Attendance Settings';
            }
        }

        function selectPrimaryMode(value) {
            const radios = form.querySelectorAll('input[name="primaryMode"]');
            let matched = false;
            radios.forEach(radio => {
                if (value && radio.value === value) {
                    radio.checked = true;
                    matched = true;
                } else if (!matched && !value && radio.hasAttribute('checked')) {
                    radio.checked = true;
                }
            });
            if (!matched && value) {
                const first = radios[0];
                if (first) first.checked = true;
            }
        }

        function hydrate(data) {
            selectPrimaryMode(data?.primaryMode || 'QR');
            if (manualToggle) {
                manualToggle.checked = !!data?.allowManualEntry;
                toggleManualOptions(manualToggle);
            }
            if (requireGeofenceToggle) {
                const requireValue = (data?.enforceGeofence ?? data?.requireGeo) || false;
                requireGeofenceToggle.checked = requireValue;
            }
            if (restrictByIpToggle) restrictByIpToggle.checked = !!data?.restrictByIp;
            if (preventDoubleClockInToggle) preventDoubleClockInToggle.checked = data?.preventDoubleClockIn ?? true;
            if (autoClockOutToggle) {
                autoClockOutToggle.checked = !!data?.autoClockOutEnabled;
                toggleAutoClockOut(autoClockOutToggle);
            }
            if (autoClockOutHours && data?.autoClockOutMaxHours) {
                autoClockOutHours.value = Number(data.autoClockOutMaxHours).toString();
            }
            if (defaultShiftInput) defaultShiftInput.value = data?.defaultShiftTemplateCode || '';
        }

        async function loadSettings() {
            try {
                const resp = await fetch(`${apiBase}/api/org-attendance-settings/current`, {
                    headers: { 'Authorization': `Bearer ${token}` }
                });
                const text = await resp.text();
                const data = text ? JSON.parse(text) : null;
                if (!resp.ok) {
                    showError(data?.message || 'Unable to load attendance settings.');
                    return;
                }
                hydrate(data);
            }
            catch {
                showError('Unable to load attendance settings.');
            }
        }
        const instructionBindings = [
            { el: manualToggle, modalId: 'manualEntryInstructionModal' },
            { el: requireGeofenceToggle, modalId: 'geofenceInstructionModal' },
            { el: restrictByIpToggle, modalId: 'ipRestrictionInstructionModal' },
            { el: preventDoubleClockInToggle, modalId: 'doubleClockInstructionModal' },
            { el: autoClockOutToggle, modalId: 'autoClockInstructionModal' }
        ];

        instructionBindings.forEach(binding => {
            if (!binding.el) return;
            binding.el.addEventListener('change', (evt) => {
                if (evt.target.checked) {
                    openInstructionModal(binding.modalId);
                }
            });
        });

        function getSelectedMode() {
            const selected = form.querySelector('input[name="primaryMode"]:checked');
            return selected ? selected.value : null;
        }

        function parseHours() {
            if (!autoClockOutHours) return null;
            const value = autoClockOutHours.value.trim();
            if (!value) return null;
            const parsed = parseFloat(value);
            return Number.isFinite(parsed) ? parsed : null;
        }

        async function saveSettings() {
            if (isSaving) return;

            const payload = {
                primaryMode: getSelectedMode(),
                allowManualEntry: manualToggle?.checked ?? false,
                requireGeo: requireGeofenceToggle?.checked ?? false,
                enforceGeofence: requireGeofenceToggle?.checked ?? false,
                restrictByIp: restrictByIpToggle?.checked ?? false,
                preventDoubleClockIn: preventDoubleClockInToggle?.checked ?? false,
                autoClockOutEnabled: autoClockOutToggle?.checked ?? false,
                autoClockOutMaxHours: null,
                defaultShiftTemplateCode: defaultShiftInput?.value?.trim() || null
            };

            const hours = parseHours();
            if (payload.autoClockOutEnabled) {
                payload.autoClockOutMaxHours = hours;
                if (!hours) {
                    showError('Enter the number of hours before auto clock-out.');
                    autoClockOutHours?.focus();
                    return;
                }
            }

            if (!payload.primaryMode) {
                showError('Select a primary attendance mode.');
                return;
            }

            showError('');
            setSaving(true);
            try {
                const resp = await fetch(`${apiBase}/api/org-attendance-settings/current`, {
                    method: 'PUT',
                    headers: {
                        'Authorization': `Bearer ${token}`,
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(payload)
                });
                const text = await resp.text();
                let data = null;
                if (text) {
                    try { data = JSON.parse(text); } catch { }
                }
                if (!resp.ok) {
                    const message = data?.message || 'Failed to save attendance settings.';
                    throw new Error(message);
                }
                await loadSettings();
                window.showToast?.('Attendance settings updated.', true);
            }
            catch (err) {
                showError(err.message || 'Failed to save attendance settings.');
                window.showToast?.('Unable to save attendance settings.', false);
            }
            finally {
                setSaving(false);
            }
        }

        form.addEventListener('submit', function (evt) {
            evt.preventDefault();
            saveSettings();
        });

        loadSettings();
    })();
</script>
